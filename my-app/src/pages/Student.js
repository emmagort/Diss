export default function Student() {
    return (
        <div>
        <h1>Student</h1>
        <div>
            <h2>this should be the page where students can take the quizes</h2>
        </div>
        </div>
    );
    }